enum Enum {
	one = 1;
	two = 2;
	three = 3;
}

message TestMessage {
  required int32 integer = 1;
  optional string string = 2;
  repeated string repeatedString = 3;

  message Nested {
  	required float float = 1;
  }

  required Nested requiredNested = 4;
  required Enum requiredEnum = 5;
}
